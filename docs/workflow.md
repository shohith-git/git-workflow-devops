# Git Workflow

This project follows a standard Git workflow using:

- main
- dev
- feature branches

All new features are developed in feature branches and merged into the development branch before reaching production.